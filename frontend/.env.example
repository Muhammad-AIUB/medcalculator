NEXT_PUBLIC_API_URL=http://localhost:5000/api
NEXT_PUBLIC_APP_URL=http://localhost:5001
NEXT_PUBLIC_APP_NAME=Pocket Medical Calculator
NEXT_PUBLIC_APP_VERSION=1.0.0

# Google Apps Script Web App URL that the subscribe form posts to.
# Without it the form silently queues every submission for a retry that never succeeds.
NEXT_PUBLIC_SUBSCRIBE_URL=
